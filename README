Build the Docker image: `docker build -t mt-scan -f docker/Dockerfile .`
Enter container: `docker -it --rm -v mt-scan:/workspace mt-scan`
Build the project: `cmake -S . -B build -GNinja && cmake --build build`